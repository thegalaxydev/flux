[package]
name = "flux_player"
version = "0.1.0"
edition = "2024"

[dependencies]
flux_game.workspace = true
flux_runtime.workspace = true
flux_view.workspace = true
eframe = "0.32"
glam.workspace = true

[target.'cfg(windows)'.build-dependencies]
winresource = "0.1"
