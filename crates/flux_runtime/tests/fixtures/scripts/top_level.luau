-- A script that runs from the Scripts container: it isn't attached to any
-- Workspace/Gui object, so `script.Parent` is the Scripts service itself.
print("top-level ran under " .. script.Parent.ClassName)
